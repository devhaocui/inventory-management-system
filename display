#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <tuple>
#include <algorithm>

void display() {
    std::ifstream file("item.csv");
    std::vector<std::tuple<std::string, int, std::string>> items;
    std::string line, itemName, category;
    int quantity;
    
